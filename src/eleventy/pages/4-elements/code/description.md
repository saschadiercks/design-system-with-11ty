We can use `<code>` when we're in need to display code. Per default `<code>` is an inline-block element, so we can apply margin to it. `<code>`can be extended with an [utility-class](../utilities/#block) like `sdi-block`.
